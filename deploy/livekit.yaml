port: 7880
bind_addresses:
  - 0.0.0.0

rtc:
  tcp_port: 7881
  # Keep these in sync with LIVEKIT_RTC_PORT_RANGE_* in .env/.env.example.
  port_range_start: 50000
  port_range_end: 50010

turn:
  enabled: false
